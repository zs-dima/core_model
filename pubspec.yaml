# ignore: prefer-publish-to-none, this package is published.
name: core_model
description: "Shared primitives with no Flutter dependency: an ownership lease, a cancellation token, a retry backoff, an injectable clock and staged-initialization timings."

version: 0.1.0

homepage: https://github.com/zs-dima/core_model
repository: https://github.com/zs-dima/core_model
issue_tracker: https://github.com/zs-dima/core_model/issues

topics:
  - lease
  - cancellation
  - backoff
  - retry
  - primitives

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.13.4

dependencies:
  # Annotations only (`@immutable`, `@visibleForTesting`).
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0

  lints_tool: ^1.1.1
